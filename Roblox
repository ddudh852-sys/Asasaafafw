-- Remote packet size(~): 10 bytes

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local UsePickaxe = ReplicatedStorage.Remotes.UsePickaxe -- RemoteFunction 

UsePickaxe:InvokeServer(
    CFrame.new(176, 1308, -614, -1, 0, -1, 0, 1, -0, 1, -0, -1)
)
